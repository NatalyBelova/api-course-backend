PRACTICE_CRITERIA: dict[str, list[str]] = {
    "catalog": [
        "Категории и товары возвращаются с полным набором полей (id, name, price, stock, rating, category)",
        "Неактивные товары не попадают в публичный каталог",
        "Фильтрация по категории (slug) отдаёт только товары этой категории",
        "Фильтрация по цене (min_price/max_price) включительно и min_price > max_price → 400",
        "Поиск по названию регистронезависимый и работает по частичному совпадению",
        "Несколько фильтров комбинируются через AND, а не OR",
        "Карточка товара: id в ответе совпадает с id в запросе, структура соответствует Swagger",
        "Негативные сценарии покрыты: 404 несуществующий товар, 422 неверный тип id, 422 нечисловой query parameter",
        "422 (валидация схемы) не перепутан с 400 (бизнес-правило) и наоборот",
        "В таблице — конкретные запросы и фактические response, а не общие формулировки вида «работает корректно»",
    ],
    "auth": [
        "Регистрация → 201, ответ содержит user, access_token, token_type = Bearer",
        "Поле password никогда не возвращается в response (ни при регистрации, ни в GET /me)",
        "Нарушение схемы (name < 2 символов, password < 6 символов) → 422, не 400",
        "Login выдаёт новый access_token, отличный от токена при регистрации",
        "Токен из login и из регистрации одинаково даёт доступ к защищённым endpoint'ам",
        "GET /me возвращает данные текущего пользователя и совпадает с зарегистрированными данными",
        "Негативные сценарии покрыты: 409 занятый email, 401 неверный пароль, 401 без token, 401 невалидный token",
        "401 не перепутан с 403 для отсутствующего или невалидного токена",
    ],
    "cart-orders": [
        "Добавление товара в корзину → 201, CartItemResponse с корректным item_total",
        "total_price корзины равен сумме item_total всех позиций",
        "PATCH и DELETE меняют только целевую позицию, остальные позиции не затронуты",
        "price_at_purchase фиксируется в заказе и не меняется при последующем изменении цены товара",
        "После создания заказа корзина полностью очищается",
        "Допустимые переходы статуса — только created→cancelled и created→completed, всё остальное → 409",
        "Негативные сценарии покрыты: 422 quantity ≤ 0, 404 несуществующий товар, 400 превышение stock, 409 повторное добавление, 400 заказ из пустой корзины, 403 доступ к чужому заказу, 401 без token",
        "422 (валидация quantity) не перепутан с 400 (превышение stock) — это разные случаи",
    ],
    "reviews-bug-hunting": [
        "GET отзывов работает без token; для товара без отзывов — пустой массив, а не ошибка",
        "POST отзыва: rating обязателен и в диапазоне 1–5, text — необязательное поле",
        "Один пользователь может оставить только один отзыв на товар, разные пользователи — могут",
        "Негативные сценарии покрыты: 422 rating вне диапазона, 409 повторный отзыв, 404 несуществующий товар, 401 без token, 422 text длиннее 500 символов",
        "Bug hunting: найден и воспроизведён минимум один реальный баг buggy-endpoint'ов, а не просто «странный ответ»",
        "Bug report оформлен по шаблону (Title, Preconditions, Steps to reproduce, Actual/Expected result, Environment, Evidence)",
        "В отчёте явно указана разница между фактическим поведением API и документацией/ожидаемой бизнес-логикой",
    ],
}
